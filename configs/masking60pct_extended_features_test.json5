{
  // ---------------------------------------------------------------------------
  // Masking + extended-features test: same setup as masking60pct_fix_test.json5
  // (60% node mask ratio, full 9M dataset, 60 epochs, 8-GPU DDP, all this
  // session's fixes baked in) except num_features 24 -> 26, using the
  // precomputed_9M_data_extended shard set (Gasteiger partial charge +
  // topological eccentricity appended per atom).
  //
  // Purpose: the KHOP-augmentation comparison (weight_decay_fix_test vs
  // extended_features_test, both 60 epochs) showed the extended features
  // consistently HURT downstream MLP/RF performance across LIPO/Tox21/BACE
  // (LIPO R2 roughly halved, embeddings even lost to ECFP fingerprints on
  // LIPO where the 24-feature model had clearly won). This run checks
  // whether that same regression shows up under a structurally different
  // augmentation (masking) or whether it's KHOP-specific.
  // ---------------------------------------------------------------------------
  name: "MASKING60PCT_EXTENDED_FEATURES_TEST_60EP",

  head_type: "dino",
  encoder_type: "GAT",
  global_pooling: "add",

  // ---------------------------------------------------------------------------
  // Data loading -- the 26-feature shard set, staged to scratch by
  // run_masking60pct_extended_test.sbatch.
  // ---------------------------------------------------------------------------
  data_path: "data/zinc/zinc_9M_data",
  use_precomputed: true,
  precomputed_data_path: "/scratch/local/precomputed_9M_data_extended_mask60",
  cache_data_in_memory: false,
  precomputed_cache_in_memory: false,
  precomputed_max_cached_shards: 3,
  explicit_hydrogens: true,
  encode_hydrogen_count: false,

  // ---------------------------------------------------------------------------
  // Augmentation -- functional_group_masking at 60% node mask ratio, same as
  // masking60pct_fix_test.json5.
  // ---------------------------------------------------------------------------
  local_views: 2,
  local_augmentation_mode: "functional_group_masking",
  k_hops: 2,
  node_mask_ratio: 0.60,
  feature_mask_ratio: 0,
  use_batched_mask_collate: true,

  // ---------------------------------------------------------------------------
  // Runtime and logging
  // ---------------------------------------------------------------------------
  seed: 1,
  device: "cuda",
  num_workers: 6,
  loader_debug: false,
  profile_timing: true,
  profile_log_every_n_batches: 200,

  // ---------------------------------------------------------------------------
  // Model dimensions -- 26 features (24 base + Gasteiger charge +
  // topological eccentricity), everything else identical to production.
  // ---------------------------------------------------------------------------
  num_features: 26,
  edge_features: 12,
  hidden_dim: 1024,
  num_layers: 3,
  dropout: 0.0,
  epsilon: 0.0,
  projection_hidden_dim: 1024,
  projection_bottleneck_dim: 1024,
  projection_output_dim: 1024,
  projection_layers: 2,

  // Must be true: shards above were built with this flag on (26-dim node
  // features), and this also tells the online evaluator (which builds LIPO
  // graphs live from SMILES, not from the shards) to use matching 26-dim
  // featurization -- without it, online eval crashes the first time it runs
  // after epoch 1 (exact bug hit and fixed during the KHOP extended-features run).
  use_extended_features: true,

  // ---------------------------------------------------------------------------
  // Training and optimization -- identical to masking60pct_fix_test.json5
  // ---------------------------------------------------------------------------
  num_epochs: 60,

  // Per-GPU batch size. With 8 GPUs (DDP), global effective batch = 1024,
  // matching the production run's single-GPU batch_size=1024.
  batch_size: 128,

  // Explicit LR matching what auto_scale_lr computed for the production
  // run's global batch of 1024: lr_scale_base(0.00000625) * 1024/256 = 0.000025.
  auto_scale_lr: false,
  learning_rate: 0.000025,
  use_data_parallel: false,
  lr_scale_base: 0.00000625,
  lr_scale_reference_batch_size: 256,
  weight_decay: 0.04,
  weight_decay_start: 0.04,
  weight_decay_end: 0.2,

  // Production's exact (inverted) values -- already shown not to matter.
  teacher_temp: 0.07,
  teacher_temp_final: 0.01,
  teacher_temp_warmup_epochs: 20,

  student_temp: 0.1,
  teacher_momentum: 0.99,
  center_momentum: 0.9,
  sync_teacher_batchnorm_buffers: true,
  warmup_epochs: 20,
  final_learning_rate: 0.00001,

  validation_enabled: true,
  validation_split: 0.1,

  online_eval_enabled: true,
  online_eval_every_n_epochs: 1,
  online_eval_datasets: "lipo",
  online_eval_fixed_k: 21,
  online_eval_top_k_checkpoints: 5,
}
